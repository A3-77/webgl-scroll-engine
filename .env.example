# 火山引擎视觉智能（cv）—— EntitySegment 实体分割
# 控制台 → 密钥管理：https://console.volcengine.com/iam/keymanage/
#
# 用法：复制本文件为 .env 并填入真实值。
#   .env 已在 .gitignore 里，不会被提交 —— 不要把密钥写进任何其他文件。
#
# 也可以不建文件，直接注入环境变量：
#   set VOLC_ACCESS_KEY=xxx && npm run build-assets -- --provider=volcengine

VOLC_ACCESS_KEY=
VOLC_SECRET_KEY=
